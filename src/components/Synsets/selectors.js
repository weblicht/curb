// Synsets/selectors.js
// Data selectors for synsets 

export function selectSynsets(globalState, ownProps) {
    // TODO: implementation here depends on backend API changes that are not ready yet
    // Specifically, there is no API endpoint to look up synsets by ID, etc.
    return undefined;
}
   
